import React from 'react';
import Loader from 'react-loader-spinner';

const Spinner = () => {
  return <Loader type="BallTriangle" color="#9a58b2" height={100} width={100} />;
};

export default Spinner;
